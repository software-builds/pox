@import(HEADER)

@name(MAIN)
template{
    x: BOOL
}:

code:
    logs.@state([
        'Nice',
        'Wie gut'
    ])
/code

div:
    {x}.&&.HEADER.{0, "headingText"}:
        h3.{_"lustig"}
    /HEADER
    --
    {logs}.map[item -> (
        p.{item::message}
    )]
/div


/template

RUNNER::MAIN.{true}
